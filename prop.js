const qs=new URLSearchParams(location.search);const id=qs.get('id');const p=(window.dashboardData.props||[]).find(x=>x.id===id)||window.dashboardData.props[0];const set=(id,v)=>document.getElementById(id).textContent=v;set('propTitle',`${p.player} — ${p.market}`);set('propTime',`${p.day} · ${p.time}`);set('avatar',p.team);set('position',`${p.team} · ${p.pos}`);set('playerName',p.player);set('teamGame',p.game);set('market',p.market);set('price',p.price||'');set('line',p.line);set('projection',p.projection);set('edge',`Edge: ${p.edge}`);document.getElementById('propStatus').innerHTML=`<span class="status ${p.status}">${p.status}</span>`;set('confidence',`${p.confidence} confidence`);document.getElementById('reasons').innerHTML=p.why.map((x,i)=>`<div class="reason"><div class="num">${i+1}</div><p>${x}</p></div>`).join('');document.getElementById('rolePreview').innerHTML=p.role.map(x=>`<div class="checkline">✓ ${x}</div>`).join('');set('priceDiscipline',`Current tracked number: ${p.line} ${p.price||''}. This page will not treat the player name as the bet. Every update rechecks whether the exact line and juice still create value.`);set('bottom',p.bottom);document.getElementById('roleLong').innerHTML=p.role.map(x=>`<div class="audit-row"><b>✓</b><span>${x}</span></div>`).join('');document.getElementById('matchupLong').innerHTML=p.matchup.map(x=>`<div class="audit-row"><b>•</b><span>${x}</span></div>`).join('');document.getElementById('riskLong').innerHTML=p.risks.map(x=>`<div class="audit-row risk"><b>!</b><span>${x}</span></div>`).join('');set('audit',`${p.audit}. A prop cannot become BET unless the role/context, injury, matchup, price and market audits are all complete. If current-role information is unclear, the system must stay WATCH or PASS.`);document.querySelectorAll('.tabs button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.tabpane').forEach(x=>x.classList.add('hidden'));document.getElementById('tab-'+b.dataset.tab).classList.remove('hidden')}));