// Public READ-ONLY API address only. No passwords, tokens, or sportsbook API keys here.
// Leave empty for a self-contained exported snapshot; set to your Worker origin after deployment.
window.STATPIG_CONFIG = {apiBase: ''};
