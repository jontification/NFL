const qs=new URLSearchParams(location.search);const id=qs.get('id');const g=(window.dashboardData.games||[]).find(x=>x.id===id)||window.dashboardData.games[0];
const set=(id,v)=>document.getElementById(id).textContent=v;
set('gameTitle',`${g.awayName} @ ${g.homeName}`);set('gameTime',`${g.day} · ${g.time}`);set('awayBadge',g.away);set('homeBadge',g.home);set('awayName',g.awayName);set('homeName',g.homeName);document.getElementById('gameStatus').innerHTML=`<span class="status ${g.status}">${g.status}</span>`;set('marketLine',`Current Line: ${g.market}`);set('confidence',g.confidence);set('audit',g.audit);set('spread',g.market);set('total',g.total);set('snapshotStatus',g.status);set('injuryNote',g.injuryNote);set('marketNote',g.marketNote);set('bottom',g.bottom);
document.getElementById('reasons').innerHTML=g.why.map((x,i)=>`<div class="reason"><div class="num">${i+1}</div><p>${x}</p></div>`).join('');
document.getElementById('matchups').innerHTML=g.matchup.map((x,i)=>`<div class="meterrow"><div class="label">${x}</div><div class="meter">${Array.from({length:7},(_,n)=>`<span class="bar ${n<(4-(i%2))?'on':''}"></span>`).join('')}</div></div>`).join('');
document.getElementById('watchList').innerHTML=g.watch.map(x=>`<li>${x}</li>`).join('');
set('matchupLong',g.matchup.map(x=>`• ${x}`).join('\n'));document.getElementById('matchupLong').style.whiteSpace='pre-line';set('injuryLong',`${g.injuryNote} This page will be expanded during each “run the numbers” cycle with confirmed starters, replacements, cluster effects and expected point adjustments.`);set('marketLong',`${g.marketNote} We will add opening line, current line, our fair line, closing line and CLV once the weekly model is frozen.`);set('anglesLong',`Current angle: ${g.quick} This remains ${g.status} until the required audit is complete. Secondary markets such as totals, team totals and props are evaluated only when they better express the thesis.`);set('scriptLong',`The final game-script section will be generated after the injury, weather, pace and matchup audits are complete. For now, the decisive variables are: ${g.watch.join('; ')}.`);
document.querySelectorAll('.tabs button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.tabpane').forEach(x=>x.classList.add('hidden'));document.getElementById('tab-'+b.dataset.tab).classList.remove('hidden')}));
